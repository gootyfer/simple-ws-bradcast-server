simple-ws-bradcast-server
=========================

a simple websocket server for a chat

Usage:
* use ```npm install``` to install dependencies
* to lauch the server in port 3000 use ```node server.js```
